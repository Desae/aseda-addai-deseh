name: gradpath
channels:
  - defaults
  - conda-forge

dependencies:
  - python=3.11
  - pip
  - pip:
      - google-generativeai>=0.5.0
      - python-dotenv>=1.0.1
      - requests>=2.31.0
      - typing_extensions>=4.9.0
